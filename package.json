{
  "name": "dtolator-typecheck",
  "version": "1.0.0",
  "description": "TypeScript type checking environment for dtolator test suite",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "typecheck:watch": "tsc --noEmit --watch"
  },
  "dependencies": {
    "typescript": "^5.7.0",
    "zod": "^4.0.0",
    "@types/node": "^20.0.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
